{
  "name": "envisalink-syslog-listener",
  "version": "1.2.4",
  "description": "Syslog listener for the EyezOn EnvisaLink 4 (EVL4) module. Logs zone events (door open/close, arm/disarm, alarms, CID events) over UDP with optional email alerts (Mailgun), push notifications (ntfy.sh), Google Sheets logging, and configurable alert rules.",
  "main": "envisalink-syslog-listener.js",
  "scripts": {
    "start": "node envisalink-syslog-listener.js",
    "start:debug": "node envisalink-syslog-listener.js --debug --dryRun",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "form-data": "^4.0.4",
    "mailgun.js": "^12.7.1",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "jest": "^30.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
